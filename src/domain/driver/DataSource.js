const DataSource = class {
  constructor() {}

  createDataBase(dbname, credential) {
    // create database owner user
    // create database
    // register database
  }

  listDataBase() {
    // fetch enTangler database list from statics table
  }

  deleteDataBase(dbname, credential) {}
}
